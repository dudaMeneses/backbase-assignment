version: '3.3'

services:
  database:
    image: 'mysql:8.0'
    restart: always
    environment:
      MYSQL_DATABASE: 'movies_info'
      MYSQL_USER: 'mysql'
      MYSQL_PASSWORD: 'mysql'
      MYSQL_ROOT_PASSWORD: 'mysql'
    ports:
      - '3306:3306'
    expose:
      - '3306'
  cache:
    image: 'redis:6.2.7'
    restart: always
    command: redis-server
    ports:
      - '6379:6379'
    expose:
      - '6379'
